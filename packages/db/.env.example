# Connection string used by Prisma (migrations, generate, studio, seed).
# Matches the credentials in the repo-root docker-compose.yml.
DATABASE_URL="postgresql://disciplineos:disciplineos@localhost:5432/disciplineos?schema=public"
